# Source logos (Browse card badges)

Drop two small PNGs here (square, ~64×64, transparent background):

- `imdb.png`  — shown on IMDB/Cinemeta cards
- `video.png` — shown on Cinema/EgyBest cards

Until they exist the card shows a text/icon fallback, so the app still builds.
